<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res/com.nilhcem.hostseditor"
    android:id="@+id/scrollView1"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical" >

        <!-- IP (label) -->
        <com.nilhcem.hostseditor.widget.TypefacedTextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/add_host_ip"
            android:textSize="@dimen/add_host_label_size"
            app:typeface="fonts/Roboto-Regular.ttf" />

        <!-- IP (input) -->
        <EditText
            android:id="@+id/addHostIp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/add_host_form_margin"
            android:digits="0123456789.:"
            android:ems="10"
            android:inputType="textNoSuggestions" >

            <requestFocus />
        </EditText>

        <!-- Hostname (label) -->
        <com.nilhcem.hostseditor.widget.TypefacedTextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/add_host_name"
            android:textSize="@dimen/add_host_label_size"
            app:typeface="fonts/Roboto-Regular.ttf" />

        <!-- Hostname (input) -->
        <EditText
            android:id="@+id/addHostName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/add_host_form_margin"
            android:ems="10"
            android:inputType="textNoSuggestions" />

        <!-- "Add Host" Button -->
        <com.nilhcem.hostseditor.widget.TypefacedButton
            android:id="@+id/addHostButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="10dp"
            android:drawableLeft="@drawable/ic_action_add_dark"
            android:drawablePadding="8dp"
            android:text="@string/add_host_button"
            app:typeface="fonts/Roboto-Light.ttf" />
    </LinearLayout>

</ScrollView>
